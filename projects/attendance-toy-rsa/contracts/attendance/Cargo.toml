[package]
name = "attendance"
version = "0.0.1"
edition = "2024"
authors = ["Dev3Pack"]
license = "Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
